# Flask is required for the application 1
Flask==2.3.2

# Steps to run this app in a container:
# 1. Build the Docker image: docker build -t flask-hello-app .
# 2. Run the container: docker run -d -p 5000:5000 --name flask-app  flask-hello-app


